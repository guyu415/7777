# 项目说明（写给非程序员看）

> 生成时间：2026-08-04
> 当前分支：`claude/project-structure-pwa-audit-drnb34`
>
> 最近 5 条提交记录：
> 1. `5d03bff` Add lightweight dynamic heart state（新增一套轻量的"心情/亲密度"状态系统）
> 2. `183c505` Add compact check-in status and dedupe state（新增精简版"查岗"状态和防重复发送逻辑）
> 3. `28c65ce` Merge pull request #95（合并了一个 PR）
> 4. `f27fe1f` Add one-minute Shadowrocket cron test module（新增一个每分钟触发一次的测试用定时任务配置）
> 5. `9a8ee25` Add minimal Shadowrocket cron notification test（新增一个最简单的定时通知测试脚本）

## 先说结论：这不是一个 PWA

在开始之前必须先纠正一个前提：**这个项目里没有网页、没有用户界面（除了两个纯文字说明页），也没有 manifest 文件或 Service Worker（这两个是 PWA「渐进式网页应用」的必备组件）。** 我在全项目范围搜索过 `manifest`、`service worker`、`workbox` 等关键词，没有任何匹配。

这个项目实际上是一个**跑在云端、没有界面的"机器人后台服务"**，专业说法是：一个部署在 Cloudflare Workers（一种"无需自己管服务器"的云计算平台，代码上传后由 Cloudflare 负责运行）上的 **MCP 服务器**。

MCP（Model Context Protocol，模型上下文协议）是一种让 AI 助手（比如 Claude、ChatGPT）能够"调用工具"的协议。这个项目本质上是给 AI 助手提供了一套"工具箱"：AI 助手连接上这个服务后，就能帮用户远程开关空调、查看手机状态、查网易云在听什么歌、B站在看什么视频等等。

---

## 技术栈

- **运行平台**：Cloudflare Workers（一种"无服务器"云平台，代码写好上传即可运行，不用自己买服务器、配置系统）。
- **编程语言**：TypeScript（一种给 JavaScript 加上"类型检查"的语言，能在写代码阶段就发现很多低级错误）。
- **核心框架**：
  - `@modelcontextprotocol/sdk`（官方 MCP 协议开发包，用来搭建"AI 工具箱"服务）
  - `agents`（Cloudflare 提供的一个库，帮助管理"有状态"的 AI 智能体，比如记住空调当前是开是关）
  - `@cloudflare/workers-oauth-provider`（处理 OAuth 授权登录的库，用来验证"谁有权限连接这个服务"）
  - `zod`（一个用来校验数据格式是否合法的库，比如校验"温度必须在 16-30 度之间"）
- **数据存储**：Cloudflare 的 **Durable Objects**（一种云端小型数据库/内存单元，专门用来保存"少量但需要持续更新"的状态，比如空调当前温度、手机最后一次上报的位置）配合 **KV**（一种简单的键值对存储，这里只用来存 OAuth 授权令牌）。
- **没有传统意义上的"前端"**：没有 React/Vue 等界面框架，也没有 CSS 框架。项目里唯一的"网页"是两小段直接写在代码字符串里的 HTML（一个是首页说明，一个是"要不要授权访问"的确认页面），谈不上真正的前端工程。
- **没有数据库**（如 MySQL、MongoDB 那种），所有数据都存在 Cloudflare 的 Durable Objects 里。
- **依赖大量第三方 API（后端确实会主动对外发起网络请求）**：
  - **涂鸦（Tuya）智能家居云 API**：用来控制一台红外空调（本质是模拟遥控器发红外信号）。
  - **网易云音乐**（未公开的内部接口）：查询"最近播放了什么歌"，甚至能估算当前播放到哪句歌词。
  - **B 站（哔哩哔哩）**：查询"最近在看什么视频/番剧"以及观看进度。
  - **高德地图（AMap）**：把手机上报的经纬度坐标转换成"在哪条路附近、哪个小区"这样的文字描述。
  - **一个自建的"记忆系统"**（`chat.xiaoman.xyz`，不在本项目代码里，属于外部服务）：用来存取"用户手写的长期相处规则"。
- **测试工具**：Node.js 自带的测试功能（不需要额外安装测试框架），目前只有一份测试文件，覆盖"心情状态"这个模块。
- **构建/开发工具**：`wrangler`（Cloudflare 官方命令行工具，用来本地调试和把代码发布上线）。

---

## 已有功能（用普通人能懂的话说）

把自己想象成正在通过 Claude 这类 AI 助手对话，这个后台服务能让 AI 做到：

1. **远程控制空调**
   - 查看空调现在是开是关、温度多少、什么模式（制冷/制热/送风/自动/除湿）、风速多大。
   - 开机、关机。
   - 调节目标温度（限制在 16–30 摄氏度之间，防止设置离谱数值）。
   - 切换运行模式和风速档位。
   - 空调本身只能"发指令"（红外遥控是单向的，发出去不知道对方收没收到），所以如果查询失败，会退回显示"上一次记住的状态"并提示"这只是缓存，可能不准"。

2. **接收手机主动上报的各种状态**（配合 iPhone「快捷指令」App 定时自动发送数据过来）
   - 电量、是否充电、设备型号、系统版本、网络类型、专注模式状态。
   - 当前打开/关闭的 App，以及"过去 24 小时用了哪些 App、用了多久"。
   - 今日步数。
   - 手机上报的定位坐标，并自动转换成"大概在哪条路、哪个小区附近"的中文描述（会隐藏精确坐标，只给出模糊范围，照顾隐私）。
   - 当地天气（天气状况、气温、体感温度、降雨概率）。
   - **月经周期估算**：根据用户记录的经期开始日期，自动估算当前处于"月经期/卵泡期/排卵期/黄体期"哪个阶段，以及预计下次来的日期。项目里特别注明"这只是估算，不能当作避孕或医疗依据"。
   - **判断用户是否离家、离家多久**：识别手机上报的"离家"和"到家"这两个特殊事件，从而知道用户当前在不在家、这次外出开始了多久。

3. **查询"最近在干什么"**
   - 网易云音乐最近播放的歌曲，并且会推算"现在很可能仍在播放"还是"只是历史记录了"，甚至尝试估算当前播放到哪一句歌词（只显示一小段，不显示完整歌词）。
   - B 站最近观看的视频/番剧，包括标题、分集、看到第几秒、看了百分之多少，并会和上一次查询结果比较——是不是换了一集、还是接着往下看了。

4. **"主动关心"防打扰机制（专为定时任务设计的精简接口）**
   - 有一个专门给"定时任务"（比如每隔一段时间机器人主动看一眼状态）用的精简查询接口，只返回"这段时间内发生了什么变化"，避免每次都返回全部历史数据造成信息过载。
   - 记录"这一轮机器人已经发送过什么类型的关心消息了"（比如"早安""发现你出门了""下午问候""提醒早点睡"），防止短时间内对同一件事重复发送好几条消息。

5. **"轻量心情/亲密度"模拟系统（这是最近新增的功能，也是最复杂的一块）**
   - 系统会模拟一个"角色"（代码注释里称呼用户为"哥哥"）随时间推移产生的各种"心理驱力"，比如"想她、想靠近她""惦记她在做什么""想聊天""想推进没做完的事"等等，一共十几种维度。
   - 这些"驱力"数值会随真实经过的时间自动增长（比如分开越久，"想念"值越高），并且区分白天/夜晚/凌晨，增长速度不同。
   - 当系统记录"发生了一次明确的互动"（比如陪伴、亲密互动、分享、争执后和好等），对应的"驱力"数值会被消解或者调整。
   - 有一个特别设计：**"争执"只会带来有上限、会自然消退的生气和委屈情绪，永远不会变成"讨厌""想离开""冷暴力"**——这是刻意写死的产品逻辑，用代码强制保证 AI 的语气不会因为拌嘴就变得冷淡或报复性。
   - 还会记录一些"一闪而过的念头"，如果某个念头持续存在超过一定时间且强度够高，会被系统标记为"执念"，供 AI 表达时参考。
   - 这套系统完全靠本地计算时间差和公式来"演化状态"，不调用任何 AI 大模型，纯粹是数学规则。

6. **用户自定义的长期"相处规则"**
   - AI 可以读取用户之前手写并保存下来的"长期互动准则"（比如"喜欢被叫什么称呼""希望用什么语气说话"）。
   - 当用户明确要求"记住这条规则"时，AI 可以调用接口把新规则写入长期记忆（有明确限制：不能存密码、密钥、验证码等敏感信息）。

7. **身份验证与授权**
   - 使用标准的 OAuth 2.0 协议（就是常见的"用某某账号登录/授权第三方应用"那一套机制）来控制"哪个 AI 客户端可以连接这个服务"。
   - 有一个自定义的网页确认页面，列出"即将获得哪些权限"，用户需要手动点击"授权"或"拒绝"。

8. **配套的手机端自动化脚本（不在 Cloudflare 上运行，是给 Shadowrocket App 用的）**
   - `scripts/shadowrocket-bilibili.js`：这是一个运行在 **Shadowrocket**（一款 iOS 上的网络代理工具）里的脚本，能"偷看"手机上 B 站 App 正常上报播放进度的网络请求，顺手把这些数据转发一份给本项目的后台，从而实现"B站在看什么"的实时上报（不需要用户手动做任何操作）。

---

## PWA 现状

**结论：目前 PWA 相关的能力是 0，因为这根本不是一个网页应用。**

逐项对照检查结果：

| PWA 要素 | 现状 |
|---|---|
| **manifest.json**（用来告诉浏览器"这个网页可以被安装成 App 图标"的配置文件） | 不存在 |
| **Service Worker**（一段能拦截网络请求、实现离线缓存的后台脚本） | 不存在 |
| **缓存策略** | 不适用——没有前端页面需要缓存 |
| **离线能力** | 无——这个服务本身要求实时联网（要连 Cloudflare、涂鸦云、网易云、B站等），断网状态下完全无法工作，「离线」这个概念对它没有意义 |
| 唯一存在的"网页" | 两段写死在后端代码字符串里的静态 HTML：一个是服务介绍首页，一个是 OAuth 授权确认页。它们只是普通网页，没有任何 PWA 特性（不可安装、不可离线、没有推送通知等） |

如果用户真正想要的是"做一个能装到手机桌面、可以离线用的网页应用"，那么当前这个仓库跟那个目标完全不沾边，需要另起一个前端项目从零开始搭建。

---

## 未完成/可疑的地方

1. **遗留的"临时调试"代码，作者自己写了要删但还没删**
   `src/device-state.ts` 里，每次手机上报数据后，接口会把"调试信息"（收到了哪些字段、每个字段的值）一起返回给调用方。代码里的注释原话是：
   > `// debug is temporary — remove once the Shortcut's field names are settled`（调试信息是临时的——等 Shortcuts 那边的字段名稳定后就删掉）
   目前这段"临时"代码还留在正式代码里，没有被清理。

2. **空调的"模式/风速"编号是猜的，作者自己也不确定**
   `src/ac-agent.ts` 顶部注释写着：
   > `// Standard Tuya IR AC scene: 0=Auto, 1=Cool, 2=Heat, 3=Fan, 4=Dry`
   > `// Adjust if the physical device uses a different ordering.`
   意思是"这是涂鸦红外空调的标准编号，如果实际这台空调编号不一样，需要自己调整"。也就是说，这套编号并没有针对具体这台空调做过验证，存在"设置的是制热，空调实际执行了制冷"这种错配风险。

3. **一个明确写着"测试完就该删除"的定时任务，目前还留在仓库里**
   `modules/shadowrocket-cron-test.sgmodule` 文件里写：
   > `#!desc=每分钟运行一次，只发送"cron 已运行"通知；测试结束后应停用或删除。`
   配合它的 `scripts/shadowrocket-cron-test.js` 只做一件事：每分钟发一条"cron 已运行"的手机通知，纯粹是为了验证"定时任务这个机制能不能正常触发"。这是典型的一次性调试用文件，按作者自己的说明，理应在验证完后清理掉，但目前仍然存在。此外这个模块文件里还硬编码指向了另一个开发分支（`claude/worker-oauth-auth-6wxiq4`）的 GitHub 原始文件地址，如果那个分支被删除，这个定时任务会直接失效。

4. **依赖一个完全在本仓库之外、没有任何文档说明的外部服务**
   "长期相处规则"这个功能会调用 `https://chat.xiaoman.xyz/memory/list` 和 `/memory/remember` 这两个地址，但这个 `chat.xiaoman.xyz` 服务的代码根本不在这个仓库里，无法确认它的行为、稳定性或权限控制是否合理。如果这个外部服务出问题或下线，相关功能会直接报错。

5. **没有 README 文件**
   仓库根目录下没有任何 `README.md`，新人接手时完全没有"这个项目是干什么的、怎么部署、需要哪些密钥"这类基本说明，只能靠读代码和 `wrangler.toml` 里的注释自己拼凑。

6. **敏感密钥的管理依赖"口头约定"，没有强制检查**
   `wrangler.toml` 里用注释的方式提醒"`COOKIE_SECRET`、`TUYA_CLIENT_SECRET`、`DEVICE_WRITE_TOKEN`、`NCM_COOKIE`、`BILIBILI_COOKIE` 这几个密钥要单独存成 Cloudflare 的 secret，不要写在这个文件里"，但这只是靠人自觉遵守的注释，代码层面没有任何机制去检查"是不是真的所有密钥都配置好了"，一旦漏配，对应功能会在运行时才报错。

7. **自动化测试覆盖很少**
   整个项目只有一个测试文件（`test/heart-state.test.ts`，覆盖"心情状态"这一个模块），空调控制、手机状态上报、网易云/B站查询这几块核心功能完全没有自动化测试，出问题只能靠手动试。

8. **依赖版本存在已知安全提示（未逐一核实是否影响此项目）**
   执行 `npm install` 时 npm 报告发现 17 个已知安全提示（3 个低危、8 个中危、6 个高危），来自间接依赖包。这类提示常见于很多项目、不一定代表实际可被利用的风险，但目前没有看到任何处理记录（比如 `npm audit fix` 或版本升级），建议后续找懂技术的人评估一下。

---

### 附：好消息（不是全是问题）

- 项目**已经能正常跑测试**（`npm test`，6 个测试全部通过）和**类型检查**（`npm run type-check`，没有报错），说明代码本身没有明显的语法或类型错误，是"能运行"的状态。
- 代码里有相当多细致的边界处理，比如 iPhone「快捷指令」经常把字段名写得五花八门（大小写、中英文混用），`device-state.ts` 里专门做了一大张"字段名对照表"来兼容这些情况，说明作者在处理"来自不那么规范的输入源"这件事上花了不少心思。
